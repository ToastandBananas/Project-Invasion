using UnityEngine;

public class Testing : MonoBehaviour
{
    /*void Update()
    {
        if (Input.GetMouseButtonDown(0))
        {
            bool isCriticalHit = Random.Range(0, 100) < 30;
            DamagePopup.Create(Utilities.GetMouseWorldPosition(), 100, isCriticalHit);
        }
    }*/
}
